/**
 * ARSHAD SHAIKH - CREATIVE PORTFOLIO
 * 3D Web & Scroll Experience Architecture
 * Inspired by Refrence.mp4
 */

document.addEventListener('DOMContentLoaded', () => {
    // --------------------------------------------------------------------------
    // 1. EMBEDDED LOTTIE ANIMATION DATA FOR "hello"
    // --------------------------------------------------------------------------
    const HELLO_LOTTIE_DATA = {"nm":"Main Scene","ddd":0,"h":148.2813232202923,"w":374.5618305368034,"meta":{"g":"@lottiefiles/creator 1.83.0"},"layers":[{"ty":4,"nm":"katman 2 Outlines","sr":1,"st":0,"op":732,"ip":0,"hd":false,"ddd":0,"bm":0,"hasMask":false,"ao":0,"ks":{"a":{"a":0,"k":[250,250,0],"ix":1},"s":{"a":0,"k":[108.654,108.654,100],"ix":6},"sk":{"a":0,"k":0},"p":{"a":0,"k":[176.7128746070074,77.77041947221556],"ix":2},"r":{"a":0,"k":0,"ix":10},"sa":{"a":0,"k":0},"o":{"a":0,"k":100,"ix":11}},"shapes":[{"ty":"gr","bm":0,"hd":false,"mn":"ADBE Vector Group","nm":"Group 1","ix":1,"cix":2,"np":3,"it":[{"ty":"sh","bm":0,"hd":false,"mn":"ADBE Vector Shape - Group","nm":"Path 1","ix":1,"d":1,"ks":{"a":1,"k":[{"o":{"x":0.281,"y":0.613},"i":{"x":0.596,"y":1},"s":[{"c":false,"i":[[0,0],[-4.256,36.426],[2.853,-21.124],[0,0],[-18.128,-1.787],[-22.979,-0.255],[-0.766,11.745],[5.66,-16.852],[-20.044,4.321],[-1.453,15.25],[7.149,-14.297],[-24.203,-4.498],[-2.809,17.873],[8.422,-15.279],[-32.094,2.751],[-25.982,2.314],[0.854,-11.109],[10.851,1.532],[-2.587,8.901],[-6.236,0.17],[-7.915,0.128],[0,0]],"o":[[0,0],[2.427,-20.781],[-2.331,17.258],[0,0],[19.915,2.33],[20.427,0.227],[0.883,-13.542],[-5.204,15.495],[30.881,-6.659],[1.531,-16.085],[-6.678,13.357],[28.851,5.361],[2.716,-17.287],[-9.068,16.45],[26.809,-2.298],[11.664,-1.038],[-0.894,11.617],[-9.911,-1.399],[3.192,-10.978],[8.868,-0.24],[7.03,-0.113],[0,0]],"v":[[-145.66,43.747],[-81.851,-26.162],[-101.426,-23.013],[-109.596,40.561],[-85.851,1.753],[-69,40.305],[-26.873,10.943],[-50.022,11.966],[-23.54,40.581],[23.936,-26.077],[6.574,-29.397],[12.958,41.583],[67.086,-23.779],[50.234,-30.673],[59.937,41.326],[102.898,-0.05],[118.532,21.029],[95.809,40.943],[83.425,17.072],[102.898,-0.05],[124.149,5.199],[138.27,-2.922]]}],"t":300},{"s":[{"c":false,"i":[[0,0],[-4.256,36.426],[2.853,-21.124],[0,0],[-18.128,-1.787],[-22.979,-0.255],[-0.766,11.745],[5.66,-16.852],[-20.044,4.321],[-1.453,15.25],[7.149,-14.297],[-24.203,-4.498],[-2.809,17.873],[8.422,-15.279],[-32.094,2.751],[-25.982,2.314],[0.854,-11.109],[10.851,1.532],[-2.587,8.901],[-6.236,0.17],[-7.915,0.128],[0,0]],"o":[[0,0],[2.427,-20.781],[-2.331,17.258],[0,0],[19.915,2.33],[20.427,0.227],[0.883,-13.542],[-5.204,15.495],[30.881,-6.659],[1.531,-16.085],[-6.678,13.357],[28.851,5.361],[2.716,-17.287],[-9.068,16.45],[26.809,-2.298],[11.664,-1.038],[-0.894,11.617],[-9.911,-1.399],[3.192,-10.978],[8.868,-0.24],[7.03,-0.113],[0,0]],"v":[[-145.66,43.747],[-81.851,-26.162],[-101.426,-23.013],[-109.596,40.561],[-85.851,1.753],[-68.999,40.305],[-26.873,10.943],[-50.022,11.966],[-23.54,40.581],[23.936,-26.077],[6.574,-29.397],[12.958,41.583],[67.086,-23.779],[50.234,-30.673],[59.937,41.326],[102.898,-0.05],[118.532,21.029],[95.809,40.943],[83.425,17.072],[102.898,-0.05],[124.149,5.199],[136.66,-1.311]]}],"t":373}],"ix":2}},{"ty":"tm","bm":0,"hd":false,"mn":"ADBE Vector Filter - Trim","nm":"Trim Paths 1","ix":2,"e":{"a":1,"k":[{"o":{"x":0.302,"y":0.14},"i":{"x":0.665,"y":1},"s":[0],"t":25},{"s":[100],"t":300}],"ix":2},"o":{"a":0,"k":0,"ix":3},"s":{"a":1,"k":[{"o":{"x":0.053,"y":0},"i":{"x":0,"y":1},"s":[0],"t":25},{"o":{"x":0.167,"y":0},"i":{"x":0.833,"y":1},"s":[1],"t":300},{"o":{"x":0.477,"y":0},"i":{"x":0.729,"y":1},"s":[1],"t":499},{"s":[100],"t":690}],"ix":1},"m":1},{"ty":"gs","bm":0,"hd":false,"mn":"ADBE Vector Graphic - G-Stroke","nm":"Gradient Stroke 1","e":{"a":0,"k":[134.5,-3],"ix":5},"g":{"p":17,"k":{"a":0,"k":[0,1,0.34901960784313724,0,0.055,1,0.34901960784313724,0,0.11,1,0.34901960784313724,0,0.171,1,0.34901960784313724,0,0.233,1,0.34901960784313724,0,0.299,1,0.34901960784313724,0,0.365,1,0.34901960784313724,0,0.422,1,0.34901960784313724,0,0.48,1,0.34901960784313724,0,0.543,1,0.34901960784313724,0,0.605,1,0.34901960784313724,0,0.671,1,0.34901960784313724,0,0.738,1,0.34901960784313724,0,0.806,1,0.34901960784313724,0,0.875,1,0.34901960784313724,0,0.938,1,0.34901960784313724,0,1,1,0.34901960784313724,0],"ix":8}},"t":1,"a":{"a":0,"k":0},"h":{"a":0,"k":0},"s":{"a":0,"k":[-135,19],"ix":4},"lc":2,"lj":2,"ml":1,"o":{"a":0,"k":100,"ix":9},"w":{"a":1,"k":[{"o":{"x":0.681,"y":0},"i":{"x":0.788,"y":1},"s":[9],"t":25},{"o":{"x":0.059,"y":0},"i":{"x":0.118,"y":1},"s":[10],"t":73},{"s":[9],"t":300}],"ix":10}},{"ty":"tr","a":{"a":0,"k":[0,0],"ix":1},"s":{"a":0,"k":[100,100],"ix":3},"sk":{"a":0,"k":0,"ix":4},"p":{"a":0,"k":[252,245.918],"ix":2},"r":{"a":0,"k":0,"ix":6},"sa":{"a":0,"k":0,"ix":5},"o":{"a":0,"k":100,"ix":7}}]}],"ind":1}],"v":"5.7.0","fr":120,"op":720,"ip":0,"assets":[]};

    // --------------------------------------------------------------------------
    // 2. PRELOADER CONTROLLER WITH "hello" ANIMATION
    // --------------------------------------------------------------------------
    // 2. PRELOADER & OPENING BANNER SEQUENCE
    // Step 1: Dark Screen + Golden "hello" Handwriting
    // Step 2: Opening Banner (Luxury Cream/Bone Panels from Refrence.mp4)
    // Step 3: Banner splits open & Hero sections reveal
    // --------------------------------------------------------------------------
    const preloaderEl = document.getElementById('preloader');
    const helloWrap = document.querySelector('.preloader-hello-wrap');
    const openingBanner = document.querySelector('.opening-banner');
    const lottieBox = document.getElementById('lottie-hello');

    let isIntroFinished = false;

    function startOpeningBanner() {
        if (isIntroFinished) return;
        isIntroFinished = true;

        // 1. Fade out the "hello" word handwriting
        if (helloWrap) {
            helloWrap.classList.add('fade-out');
        }

        // 2. Activate the luxury cream opening banner
        if (openingBanner) {
            openingBanner.classList.add('active');
        }

        // 3. After a brief breath, smoothly split and open the banner panels
        setTimeout(() => {
            if (preloaderEl) {
                preloaderEl.classList.add('open-banner');
            }

            // 4. Reveal hero section elements with GSAP
            revealHero();

            // 5. Remove preloader from view once the panels finish sweeping
            setTimeout(() => {
                if (preloaderEl) {
                    preloaderEl.classList.add('finished');
                }
            }, 1200);
        }, 260);
    }

    function revealHero() {
        if (typeof gsap !== 'undefined') {
            gsap.from('.navbar', {
                y: -40,
                opacity: 0,
                duration: 1.2,
                ease: 'power3.out',
                delay: 0.15
            });

            gsap.from('.hero-main-title', {
                y: 80,
                opacity: 0,
                duration: 1.4,
                ease: 'power4.out',
                delay: 0.3
            });

            gsap.from('.hero-sub-title', {
                y: 50,
                opacity: 0,
                duration: 1.2,
                ease: 'power3.out',
                delay: 0.45
            });

            gsap.from('.hero-tag-pills, .hero-scroll-indicator, .hero-bottom-bar', {
                y: 30,
                opacity: 0,
                duration: 1,
                stagger: 0.12,
                ease: 'power2.out',
                delay: 0.6
            });
        }
    }

    // Initialize Lottie for "hello" animation
    let animInstance = null;
    if (typeof lottie !== 'undefined' && lottieBox) {
        try {
            animInstance = lottie.loadAnimation({
                container: lottieBox,
                renderer: 'svg',
                loop: false,
                autoplay: false,
                animationData: HELLO_LOTTIE_DATA
            });
            animInstance.setSpeed(1.6);

            // Play frames 0 to 300 where "hello" writes out completely
            animInstance.playSegments([0, 300], true);

            // When "hello" handwriting completes:
            animInstance.addEventListener('complete', () => {
                // Hold the complete golden "hello" for 450ms, then open the banner
                setTimeout(startOpeningBanner, 450);
            });
        } catch (e) {
            console.warn('Lottie initialization fallback:', e);
            setTimeout(startOpeningBanner, 1600);
        }
    } else {
        setTimeout(startOpeningBanner, 1600);
    }

    // Safety fallback timer if animation gets interrupted or blocked
    setTimeout(() => {
        if (!isIntroFinished) {
            startOpeningBanner();
        }
    }, 3200);

    // Click to skip intro directly to opening banner and hero
    if (preloaderEl) {
        preloaderEl.addEventListener('click', () => {
            startOpeningBanner();
        });
    }

    // --------------------------------------------------------------------------
    // 3. LENIS SMOOTH SCROLL & GSAP INTEGRATION
    // --------------------------------------------------------------------------
    let lenis = null;
    if (typeof Lenis !== 'undefined') {
        lenis = new Lenis({
            duration: 1.2,
            easing: (t) => Math.min(1, 1.001 - Math.pow(2, -10 * t)),
            orientation: 'vertical',
            gestureOrientation: 'vertical',
            smoothWheel: true,
            wheelMultiplier: 1.1,
            touchMultiplier: 2
        });

        function raf(time) {
            lenis.raf(time);
            requestAnimationFrame(raf);
        }
        requestAnimationFrame(raf);

        if (typeof ScrollTrigger !== 'undefined') {
            lenis.on('scroll', ScrollTrigger.update);
            gsap.ticker.add((time) => {
                lenis.raf(time * 1000);
            });
            gsap.ticker.lagSmoothing(0);
        }
    }

    // Navbar scroll blur effect
    const navbar = document.querySelector('.navbar');
    window.addEventListener('scroll', () => {
        if (window.scrollY > 60) {
            navbar.classList.add('scrolled');
        } else {
            navbar.classList.remove('scrolled');
        }
    });

    // --------------------------------------------------------------------------
    // 4. CUSTOM MAGNETIC CURSOR
    // --------------------------------------------------------------------------
    const cursorDot = document.querySelector('.cursor-dot');
    const cursorRing = document.querySelector('.cursor-ring');

    let mouseX = -100;
    let mouseY = -100;
    let ringX = -100;
    let ringY = -100;

    window.addEventListener('mousemove', (e) => {
        mouseX = e.clientX;
        mouseY = e.clientY;
        document.body.classList.remove('cursor-hidden');

        if (cursorDot) {
            cursorDot.style.left = `${mouseX}px`;
            cursorDot.style.top = `${mouseY}px`;
        }
    });

    document.addEventListener('mouseleave', () => {
        document.body.classList.add('cursor-hidden');
    });

    document.addEventListener('mouseenter', () => {
        document.body.classList.remove('cursor-hidden');
    });

    function renderCursor() {
        ringX += (mouseX - ringX) * 0.18;
        ringY += (mouseY - ringY) * 0.18;

        if (cursorRing) {
            cursorRing.style.left = `${ringX}px`;
            cursorRing.style.top = `${ringY}px`;
        }
        requestAnimationFrame(renderCursor);
    }
    requestAnimationFrame(renderCursor);

    // Hover elements interactions (links, buttons, interactive rows)
    const interactiveElements = document.querySelectorAll('a, button, .service-row, .contact-info-card, .rotating-badge-container');
    interactiveElements.forEach((el) => {
        el.addEventListener('mouseenter', () => document.body.classList.add('cursor-hover'));
        el.addEventListener('mouseleave', () => document.body.classList.remove('cursor-hover'));
    });

    // Form inputs and textareas
    const formFields = document.querySelectorAll('input, textarea, select');
    formFields.forEach((field) => {
        field.addEventListener('mouseenter', () => document.body.classList.add('cursor-input-hover'));
        field.addEventListener('mouseleave', () => document.body.classList.remove('cursor-input-hover'));
    });

    // Project cards preview hover
    const projectCards = document.querySelectorAll('.card-preview-wrap, .project-card');
    projectCards.forEach((card) => {
        card.addEventListener('mouseenter', () => {
            document.body.classList.add('cursor-card-hover');
            const cText = document.querySelector('.cursor-text');
            if (cText) cText.textContent = 'VIEW ↗';
        });
        card.addEventListener('mouseleave', () => {
            document.body.classList.remove('cursor-card-hover');
        });
    });

    // --------------------------------------------------------------------------
    // 5. HERO LIQUID WAVE DISTORTION ON "CREATIVE"
    // --------------------------------------------------------------------------
    const heroTitle = document.querySelector('.hero-main-title');
    const turbFilter = document.getElementById('liquid-turb');
    const dispMap = document.getElementById('liquid-disp');

    let currentScale = 0;
    let targetScale = 0;
    let waveFreq = 0.012;

    if (heroTitle && dispMap) {
        heroTitle.addEventListener('mouseenter', () => {
            heroTitle.classList.add('distorted');
            targetScale = 32;
        });

        heroTitle.addEventListener('mousemove', (e) => {
            const rect = heroTitle.getBoundingClientRect();
            const relX = (e.clientX - rect.left) / rect.width;
            const relY = (e.clientY - rect.top) / rect.height;

            waveFreq = 0.01 + relX * 0.02;
            targetScale = 25 + Math.sin(relY * Math.PI) * 20;

            if (turbFilter) {
                turbFilter.setAttribute('baseFrequency', `${waveFreq.toFixed(3)} 0.04`);
            }
        });

        heroTitle.addEventListener('mouseleave', () => {
            targetScale = 0;
            setTimeout(() => {
                if (targetScale === 0) heroTitle.classList.remove('distorted');
            }, 300);
        });

        function animateDistortion() {
            currentScale += (targetScale - currentScale) * 0.12;
            if (dispMap) {
                dispMap.setAttribute('scale', currentScale.toFixed(2));
            }
            requestAnimationFrame(animateDistortion);
        }
        requestAnimationFrame(animateDistortion);
    }

    // --------------------------------------------------------------------------
    // 6. FULLSCREEN MENU OVERLAY
    // --------------------------------------------------------------------------
    const hamburgerBtn = document.querySelector('.nav-hamburger');
    const menuOverlay = document.querySelector('.fullscreen-menu');
    const menuLinks = document.querySelectorAll('.menu-link');

    if (hamburgerBtn && menuOverlay) {
        hamburgerBtn.addEventListener('click', () => {
            const isOpen = menuOverlay.classList.contains('open');
            if (isOpen) {
                closeMenu();
            } else {
                openMenu();
            }
        });

        function openMenu() {
            hamburgerBtn.classList.add('active');
            menuOverlay.classList.add('open');
            if (typeof gsap !== 'undefined') {
                gsap.fromTo('.menu-link', 
                    { y: 60, opacity: 0 },
                    { y: 0, opacity: 1, duration: 0.8, stagger: 0.1, ease: 'power3.out' }
                );
            }
        }

        function closeMenu() {
            hamburgerBtn.classList.remove('active');
            menuOverlay.classList.remove('open');
        }

        menuLinks.forEach((link) => {
            link.addEventListener('click', (e) => {
                e.preventDefault();
                const targetId = link.getAttribute('href');
                closeMenu();
                if (lenis && targetId.startsWith('#')) {
                    const targetEl = document.querySelector(targetId);
                    if (targetEl) lenis.scrollTo(targetEl, { offset: -40, duration: 1.4 });
                }
            });
        });
    }

    // --------------------------------------------------------------------------
    // 7. 3D TILT EFFECT ON PORTRAIT & PROJECT CARDS
    // --------------------------------------------------------------------------
    const tiltContainers = document.querySelectorAll('.portrait-card, .card-preview-inner');
    tiltContainers.forEach((card) => {
        card.addEventListener('mousemove', (e) => {
            const rect = card.getBoundingClientRect();
            const x = e.clientX - rect.left;
            const y = e.clientY - rect.top;
            const centerX = rect.width / 2;
            const centerY = rect.height / 2;

            const rotateX = ((y - centerY) / centerY) * -10;
            const rotateY = ((x - centerX) / centerX) * 10;

            card.style.transform = `perspective(1000px) rotateX(${rotateX.toFixed(2)}deg) rotateY(${rotateY.toFixed(2)}deg) scale3d(1.02, 1.02, 1.02)`;
        });

        card.addEventListener('mouseleave', () => {
            card.style.transform = 'perspective(1000px) rotateX(0deg) rotateY(0deg) scale3d(1, 1, 1)';
        });
    });

    // --------------------------------------------------------------------------
    // 8. INTERACTIVE THREE.JS 3D TECH CANVAS (EXPERTISE SECTION)
    // --------------------------------------------------------------------------
    const canvasContainer = document.querySelector('.three-canvas-container');
    const threeCanvas = document.getElementById('three-expertise-canvas');

    if (threeCanvas && canvasContainer && typeof THREE !== 'undefined') {
        const scene = new THREE.Scene();
        const camera = new THREE.PerspectiveCamera(45, canvasContainer.clientWidth / canvasContainer.clientHeight, 0.1, 1000);
        camera.position.z = 7;

        const renderer = new THREE.WebGLRenderer({
            canvas: threeCanvas,
            alpha: true,
            antialias: true
        });
        renderer.setSize(canvasContainer.clientWidth, canvasContainer.clientHeight);
        renderer.setPixelRatio(Math.min(window.devicePixelRatio, 2));

        // Lighting
        const ambientLight = new THREE.AmbientLight(0xffffff, 0.7);
        scene.add(ambientLight);

        const goldLight = new THREE.DirectionalLight(0xe6c687, 2.5);
        goldLight.position.set(5, 5, 4);
        scene.add(goldLight);

        const fillLight = new THREE.PointLight(0xffffff, 1.2, 20);
        fillLight.position.set(-5, -3, 2);
        scene.add(fillLight);

        // Geometries
        const goldMaterial = new THREE.MeshStandardMaterial({
            color: 0xe6c687,
            metalness: 0.85,
            roughness: 0.2,
            wireframe: false
        });

        const darkWireMaterial = new THREE.MeshStandardMaterial({
            color: 0x94949e,
            metalness: 0.9,
            roughness: 0.3,
            wireframe: true
        });

        const obsidianMaterial = new THREE.MeshStandardMaterial({
            color: 0x1f1f24,
            metalness: 0.7,
            roughness: 0.15
        });

        // 5 Floating 3D Geometric Tech Symbols
        const shapes = [];

        // 1. Central Icosahedron (Core Architecture / AI)
        const icoMesh = new THREE.Mesh(new THREE.IcosahedronGeometry(1.2, 0), goldMaterial);
        icoMesh.position.set(0, 0, 0);
        scene.add(icoMesh);
        shapes.push({ mesh: icoMesh, speedX: 0.008, speedY: 0.012, basePos: { x: 0, y: 0, z: 0 } });

        // 2. Torus Knot (Motion & GSAP)
        const knotMesh = new THREE.Mesh(new THREE.TorusKnotGeometry(0.55, 0.18, 64, 16), darkWireMaterial);
        knotMesh.position.set(-2.6, 0.6, -1);
        scene.add(knotMesh);
        shapes.push({ mesh: knotMesh, speedX: 0.015, speedY: 0.01, basePos: { x: -2.6, y: 0.6, z: -1 } });

        // 3. Octahedron (Three.js / 3D Web)
        const octMesh = new THREE.Mesh(new THREE.OctahedronGeometry(0.7, 0), obsidianMaterial);
        octMesh.position.set(2.5, -0.5, -0.5);
        scene.add(octMesh);
        shapes.push({ mesh: octMesh, speedX: 0.01, speedY: 0.016, basePos: { x: 2.5, y: -0.5, z: -0.5 } });

        // 4. Ring (Web APIs & Scalability)
        const ringMesh = new THREE.Mesh(new THREE.TorusGeometry(0.75, 0.08, 16, 50), goldMaterial);
        ringMesh.position.set(-1.8, -1.2, 0.5);
        scene.add(ringMesh);
        shapes.push({ mesh: ringMesh, speedX: 0.012, speedY: 0.008, basePos: { x: -1.8, y: -1.2, z: 0.5 } });

        // 5. Dodecahedron (Creative Coding)
        const dodMesh = new THREE.Mesh(new THREE.DodecahedronGeometry(0.65, 0), goldMaterial);
        dodMesh.position.set(2.2, 1.2, -1.2);
        scene.add(dodMesh);
        shapes.push({ mesh: dodMesh, speedX: 0.014, speedY: 0.014, basePos: { x: 2.2, y: 1.2, z: -1.2 } });

        let canvasMouseX = 0;
        let canvasMouseY = 0;

        canvasContainer.addEventListener('mousemove', (e) => {
            const rect = canvasContainer.getBoundingClientRect();
            canvasMouseX = ((e.clientX - rect.left) / rect.width) * 2 - 1;
            canvasMouseY = -(((e.clientY - rect.top) / rect.height) * 2 - 1);
        });

        canvasContainer.addEventListener('mouseleave', () => {
            canvasMouseX = 0;
            canvasMouseY = 0;
        });

        let clock = 0;
        function animateThree() {
            clock += 0.02;

            shapes.forEach((item, index) => {
                item.mesh.rotation.x += item.speedX;
                item.mesh.rotation.y += item.speedY;

                // Subtle floating physics
                item.mesh.position.y = item.basePos.y + Math.sin(clock + index * 1.5) * 0.15;
                item.mesh.position.x = item.basePos.x + Math.cos(clock * 0.7 + index) * 0.08;
            });

            // Camera reacts gently to mouse
            camera.position.x += (canvasMouseX * 1.2 - camera.position.x) * 0.05;
            camera.position.y += (canvasMouseY * 1.0 - camera.position.y) * 0.05;
            camera.lookAt(0, 0, 0);

            renderer.render(scene, camera);
            requestAnimationFrame(animateThree);
        }
        requestAnimationFrame(animateThree);

        // Responsive Resize
        window.addEventListener('resize', () => {
            if (!canvasContainer) return;
            const width = canvasContainer.clientWidth;
            const height = canvasContainer.clientHeight;
            camera.aspect = width / height;
            camera.updateProjectionMatrix();
            renderer.setSize(width, height);
        });
    }

    // --------------------------------------------------------------------------
    // 9. EXPERTISE HOVER PREVIEW CARDS
    // --------------------------------------------------------------------------
    const serviceRows = document.querySelectorAll('.service-row');
    const floatingCard = document.querySelector('.floating-preview-card');
    const floatingCardImg = floatingCard ? floatingCard.querySelector('img') : null;

    if (floatingCard && floatingCardImg) {
        serviceRows.forEach((row) => {
            row.addEventListener('mouseenter', (e) => {
                const previewSrc = row.getAttribute('data-preview');
                if (previewSrc) {
                    floatingCardImg.src = previewSrc;
                    floatingCard.classList.add('visible');
                }
            });

            row.addEventListener('mousemove', (e) => {
                floatingCard.style.left = `${e.clientX + 30}px`;
                floatingCard.style.top = `${e.clientY - 20}px`;
            });

            row.addEventListener('mouseleave', () => {
                floatingCard.classList.remove('visible');
            });
        });
    }

    // --------------------------------------------------------------------------
    // 10. STICKY STACKING CARDS SCROLLTRIGGER (SECTION 03)
    // --------------------------------------------------------------------------
    if (typeof gsap !== 'undefined' && typeof ScrollTrigger !== 'undefined') {
        const pCards = gsap.utils.toArray('.project-card');

        pCards.forEach((card, index) => {
            if (index < pCards.length - 1) {
                gsap.to(card, {
                    scale: 0.93,
                    opacity: 0.5,
                    filter: 'blur(3px)',
                    ease: 'none',
                    scrollTrigger: {
                        trigger: pCards[index + 1],
                        start: 'top 85%',
                        end: 'top 15%',
                        scrub: true
                    }
                });
            }
        });
    }

    // --------------------------------------------------------------------------
    // 11. COPY EMAIL TO CLIPBOARD & TOAST
    // --------------------------------------------------------------------------
    const emailCard = document.getElementById('copy-email-card');
    const toast = document.getElementById('toast');

    if (emailCard) {
        emailCard.addEventListener('click', () => {
            const email = 'shaikharshad92316@gmail.com';
            navigator.clipboard.writeText(email).then(() => {
                showToast('Copied to clipboard: ' + email);
            }).catch(() => {
                showToast('Email: ' + email);
            });
        });
    }

    function showToast(message) {
        if (!toast) return;
        toast.textContent = message;
        toast.classList.add('show');
        setTimeout(() => {
            toast.classList.remove('show');
        }, 3200);
    }

    // --------------------------------------------------------------------------
    // 12. CONTACT FORM SUBMISSION FEEDBACK
    // --------------------------------------------------------------------------
    const contactForm = document.getElementById('contact-form');
    if (contactForm) {
        contactForm.addEventListener('submit', (e) => {
            e.preventDefault();
            const submitBtn = contactForm.querySelector('.form-submit-btn');
            const originalText = submitBtn.innerHTML;

            submitBtn.innerHTML = 'Sending... <i class="fas fa-spinner fa-spin"></i>';
            submitBtn.disabled = true;

            setTimeout(() => {
                submitBtn.innerHTML = 'Message Sent! <i class="fas fa-check"></i>';
                showToast('Thank you! Your message has been sent successfully.');
                contactForm.reset();

                setTimeout(() => {
                    submitBtn.innerHTML = originalText;
                    submitBtn.disabled = false;
                }, 3500);
            }, 1200);
        });
    }

    // --------------------------------------------------------------------------
    // 13. LIVE LOCAL TIME DISPLAY (INDIA / MUMBAI)
    // --------------------------------------------------------------------------
    const timeDisplay = document.getElementById('local-time');
    function updateIST() {
        if (!timeDisplay) return;
        const now = new Date();
        const options = { timeZone: 'Asia/Kolkata', hour: '2-digit', minute: '2-digit', second: '2-digit', hour12: true };
        const timeStr = new Intl.DateTimeFormat('en-US', options).format(now);
        timeDisplay.textContent = `${timeStr} IST`;
    }
    updateIST();
    setInterval(updateIST, 1000);

    // --------------------------------------------------------------------------
    // 14. BACK TO TOP BUTTON
    // --------------------------------------------------------------------------
    const backToTopBtn = document.querySelector('.back-to-top-btn');
    if (backToTopBtn) {
        backToTopBtn.addEventListener('click', (e) => {
            e.preventDefault();
            if (lenis) {
                lenis.scrollTo(0, { duration: 1.8 });
            } else {
                window.scrollTo({ top: 0, behavior: 'smooth' });
            }
        });
    }

    // Smooth scroll for internal links
    document.querySelectorAll('a[href^="#"]').forEach((anchor) => {
        anchor.addEventListener('click', function (e) {
            const targetId = this.getAttribute('href');
            if (targetId && targetId !== '#') {
                e.preventDefault();
                const targetEl = document.querySelector(targetId);
                if (targetEl) {
                    if (lenis) {
                        lenis.scrollTo(targetEl, { offset: -30, duration: 1.4 });
                    } else {
                        targetEl.scrollIntoView({ behavior: 'smooth' });
                    }
                }
            }
        });
    });
});
